$.when(
  addComment( "<%= j render partial: 'products/current_user_comment', locals: {comment: @comment }%>",
              "<%= j render partial: 'products/comment', locals: {comment: @comment }%>",
              "<%= j render partial: 'products/comment', locals: {comment: @product.highest_rating_comment }%>",
              "<%= j render partial: 'products/comment', locals: {comment: @product.lowest_rating_comment }%>",
              "<%= @product.compute_average %>")
).done(
  refreshProductRating()
);